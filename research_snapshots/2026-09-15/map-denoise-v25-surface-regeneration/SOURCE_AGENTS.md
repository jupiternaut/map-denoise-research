# V25：执行规则（所有 Agent 先读）

## 你现在要做什么

在 **liekkas** 上实现并实验验证“多视图证据驱动的局部表面再生”。
用户要真实几何输出和实验结果，不是更多计划、文献清单或元研究。
用户已同意大胆构造，准备睡觉；常规实现选择自行决定，不逐步请求确认。
从 START_HERE.md → TASK.md → research_plan.md → DATA_SOURCES.md → ACCEPTANCE.md 开始。
长程执行补读 LONG_RUN.md、TASK_QUEUE.md；构造受阻时读 IDEA_LAB.md，而不是回到写计划。
任务定义在 TASK.md；流程可调整，研究对象和证据口径不可悄悄改变。

## 目标身份与权限

- 唯一写入工作区：`/home/grf/Documents/Codex/2026-09-15/map-denoise-v25-surface-regeneration`。
- 首次/恢复执行核对 hostname、实际路径和 CHECKPOINT.md；不是 liekkas 就停并说明。
- 旧工程、旧运行、已有照片/相机/网格/参考一律只读；见 DATA_SOURCES.md。
- 只在新工作区写代码、环境、下载、缓存和 runs；不要运行会写旧目录的旧 runner。
- 复用代码时复制必要模块并记原路径/哈希；`PYTHONDONTWRITEBYTECODE=1`。
- 可在本工作区安装依赖、拉取官方实现、运行 CPU/GPU、做局部实验及适量数据下载。
- 不 sudo、不改驱动、不改系统 Python/旧虚拟环境，不清理别人的缓存或进程。
- 不访问 Windows 5080、Mac、其他服务器；不推送 GitHub、不新建远程仓库。
- 不搜密钥，不另开付费 API/云 GPU；很多 TOKEN 不等于额外付费服务已授权。
- 不把旧数据上传外部服务。普通官方文献/代码/许可允许的数据下载可用。
- 首轮新增下载总流量上限 10 GiB；下载前看来源/大小，不拉整个 DTU 大包。
- 工作区新增文件总量先控制在 15 GiB 内，所在分区余量低于 15 GiB 停止扩量并保存。

## 大胆构造的范围

- 可更换关联、深度候选、局部面数、支持域、曲面拟合、优化器和采样方式。
- 旧点/旧法向/旧网格可见性是初始化，不是不可推翻的物理真值。
- 不把工作退回“再调一次 alpha”、只写理论、只加可视化、只做 TLA+ 或 Agent 调度。
- 不建设 GUI/TUI/MCP/MLIR/插件平台，不重训练整场景大模型当第一步。
- 第一版允许慢；先做真的局部输出，再批处理/GPU。慢不是零成果的理由。
- 开发失败后可继续改变机制；每次写清预测、改变点、判据，保留旧失败。
- TASK.md 中的面片/plane-sweep 是起点，不是唯一允许的答案；IDEA_LAB.md 也不是候选上限。
- 可在同目标与输入权限内提出未列出的新算子、组合已有模块或放弃最初构造，不必为技术选择重新请示。
- 一轮未达到 20% 不自动结束任务：先区分真实的小收益、错误归因和实现不足，再执行队列中有信息量的工作。
- 也不能用身份回退/新增输出名字充当新算子；必须出现实际不同的可复查几何行为。

## 必须守住的证据边界

- 算法/候选选择只读照片、相机、输入几何及事先约定的元数据。
- 激光参考、官方评测掩膜只进入评价端；不能喂给构造器或用来选确认 ROI。
- 开发集几何评价可指导开发，但不得把其分数伪装成算法运行时输入。
- scan24 与 scan37 都已暴露，只叫开发数据。旧文件名含 confirm 不改变这一事实。
- 新候选、代码、配置先封存再评价；确认集在锁定方法后仅做一次主评价。
- 同几何同评分；空输出/无效输出不能获得零误差；不靠删点或增密赢。
- 变支持输出用固定空间 ROI 与统一采样规则；不能套旧 1024 行索引当新评价。
- 多视图候选深度不等于真实多层；低照片损失不等于几何正确。
- 多张原图可能都参与过原模型训练。为 V25 留出的视图不是全系统独立测试。
- 未完成外部基线要写 NOT_RUN/BLOCKED，不能拿自研近似冒充官方结果。

## 长程执行与恢复

- 默认工作窗口：首次实质执行起约 8 小时；不是必须空耗 8 小时，也不是运行保证。
- 未完成目标且有安全、有信息量的下一步时继续，不在写完计划或 smoke 后交差。
- 窗口含环境、实现、调试、实验、验收；最后约 45 分钟用于封存与报告。
- 每个长进程记命令、PID/任务 ID、日志路径、开始时间及输出目录。
- 每 20–30 分钟或重要阶段结束更新 CHECKPOINT.md；恢复先检查进程，不能重复启动。
- 正在正常计算且临近窗口结束：保存可恢复状态；不杀其他任务，不谎称完成。
- 可用队列完成可提前收尾；权限/目标身份阻塞且无替代工作时准确报告，不自作授权。
- 正常失败是继续构造的依据，不是改标题宣布获胜的依据。
- 每次准备结束当前 turn 前检查 TASK_QUEUE.md：还有可执行任务且窗口未结束就继续。
- 计划写完、一次实验失败、基线暂不可用、GPU 忙、单元测试全绿，都不是单独的停止理由。

## 并发与工具

- 能并发时用 3 个互不覆盖的角色：观测与基线 / V25 构造 / 独立评价。
- 主 Agent 集成与定方向；子 Agent 不各自另开课题，不重复整套数据准备。
- 没有子 Agent 工具就单 Agent 顺序推进、独立脚本核验，不虚构并发。
- 初始至多 4 个 CPU worker，每 worker 数值库线程 1；最多 1 个重 GPU 作业。
- 用户追加要求：**GPU 阶段只在显卡空闲时启动**；遵循 GPU_POLICY.md，不抢占现有任务。
- GPU 忙时继续 CPU 构造/测试/评价/基线准备；全窗口未空闲则保留待运行命令，不谎称 GPU 已测。
- 先核对实际工具清单/参数，再调用。不要猜 MCP 前缀、复述“缩短名无效”形成循环。
- 同类工具调用失败 2 次就读 schema 或换已确认工具；记录一次可操作错误，继续别的分支。
- native 文件/终端工具可用就用；不能调用当前环境未暴露的 Cursor 原生工具。
- 本文件是行为契约，不是操作系统沙箱。不得宣称已通过它实现隐藏参考的强安全隔离。

## 交付

必须包含能运行的代码、真实输入/输出几何、逐 ROI 指标、对照图、命令和环境、REPORT.md、CHECKPOINT.md。
不例行写导师简报/论文，不用样本输出数量冒充独立场景数量。
最后明确：实现了什么、跑了什么、有没有比同观测基线更好、没做到什么、下一条确切命令。

# 一页简报：多帧点云数据接入与预实验

> 历史 V1 简报，部分评价与解释已撤回。请使用 [导师简报 V2](/home/grf/Documents/Codex/2026-09-12/map-denoise-dataset-pilot-v1/导师简报_V2.md)。以下保留原记录。

2026-09-12，主机 liekkas。旧算法没改。没有独立真实几何分。

我们真正拿到的，是 ETH3D `delivery_area` 和 `courtyard` 各两站 laser scan。PLY 是扫描仪局部坐标，位姿只乘一次。两个场景都没有第三站重叠。另外有 27 个带帧来源的精确合成对照。UCL 走廊停在官网 OneDrive 登录墙，没有 E57，也没有编站号。TUM-TLS-24 只记了元数据，大约 95 GB 的包没下。

能评价注入偏差恢复的，是这 27 个合成，加上 6 块真实局部板（每块至少两站，300–3000 点）上的平移 / 旋转扰动。参考云仍是未扰动测量，不是独立真值。能评价独立真实几何的数据：本轮没有。ETH3D clean 和 BIM 都不能当毫米真值。`da_junction` 是同一墙上的第二条板，不是薄层标签。

旧模型对共同平移和旋转的反应不一样。`identity` 不改点，恢复误差就是注入位移，中位约 3.54 mm（平移）和 3.62 mm（再加 0.05° 旋转）。`xyz_mixture`、`fast`、`open3d_icp_then_xyz` 会把墙改写大约 4 cm，平移和旋转在「对测量云的恢复」上几乎一样。`fast` 对自己输出更稳，但一加旋转，稳定性从 3.09 mm 掉到 5.02 mm。合成上 `fast` 把全部 6 个 2 mm 双层并成一层；`identity` 把 6 个单层重影全拆开。`open3d_icp_then_xyz` 是官方 ICP 加自研后处理，不是 JRMPC/BALM，层数计数最好，间距中位误差仍有 1.88 mm。

下一刀最值得补的是简单端：联合估计层位置、每站偏差和残差噪声。依据是旧方法改写了大约 4 cm，却吃不下大约 3.5 mm 的帧偏差；2 mm 真双层还会被并掉。预先换 σ=2/5 mm，真实恢复几乎不动，所以盲估计噪声不是这一步最缺的。倾斜项可以跟着做，因为旋转已经伤了 `fast` 的稳定性。复杂端的关联和可见性先留着，等截距模型有了再加。三条线都还在。

下一轮不用重做数据准备。直接用 `/srv/slam-research/grf/map-denoise/datasets/multiscan-pilot-v1/eth3d/`、`runs/multiscan-pilot-v1/patches/`、`runs/multiscan-pilot-v1/synthetics/`，以及本目录的 `schema.py`、`PILOT_PROTOCOL.md`、`PILOT_RESULTS.csv`。UCL 和 TUM 仍缺文件。旧检查点只读，哈希没变。

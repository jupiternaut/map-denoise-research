# V2 修复协议（运行前确定）

本轮只修评价、数据契约、复现入口与报告，不改变四个冻结算法，不搜索新超参数。

旧项目与结果归档在 `/srv/slam-research/grf/map-denoise/runs/multiscan-pilot-v1/checkpoints/pre-repair-Y8ZRZZ/`。原 `pilot/`、`patches/`、`synthetics/` 及旧 CSV 保持不变。

1. 合成几何评分使用世界坐标、生成器声明的有限平面矩形。计算输出到真实表面的均值/RMS/P95 距离；真实采样点到输出的 1/2 mm 覆盖率（采样相关，不是全表面覆盖）。不从 SSE 强制挑 K，也不让算法元数据改变几何分数。
2. 原点 ID 对应的法向误差、XYZ 误差及按真实来源层分组的均值间距另作对应关系诊断。均值间距保持不代表两个物理表面被恢复。自报 K 只作模型决策信息，不等同误拆/误并。不可辨识案例保留输出，不评唯一真值。
3. 27 例合成在新目录重新生成，断言算法输入世界坐标、帧号和真值点与旧版完全相同；只修扫描局部坐标/位姿一致性，并补评价表面描述。旧 108 个合成输出直接复用重新评分。
4. 旧 336 个真实输出保留并重评，明确标记为 `fixed_reference_diagnostic`（来自未扰动参考点云的坐标系）。新增同一六片区、两档 sigma、同样扰动种子的 `current_input` 版本，坐标系只从该次可见点估计。不改变片区与随机种子。
5. 对真实片区同时报告无扰动改写、扰动后与原测量云之差，以及对自身无扰动输出的响应；不能把前者归因于新注入偏差，也不能把参考测量云当真实几何。
6. 平移参数 5 mm 是非参考站的加权 RMS；固定参考站后全体 RMS 会更低。报告实际点位移；保留既有旋转中心与扰动模型，不更改实验后再比较。
7. 修复测试先于实验：精确真值零误差、100 mm 错位非零、元数据不改变几何评分、点序置换不改变几何评分、双层塌缩有对应结构损失、世界坐标评价不受算法局部坐标系影响、局部到世界一致、射线一致、防覆盖。
8. 全部新产物使用唯一运行目录。运行前后校验旧数据、旧结果及冻结算法源码哈希。报告复用与新计算分别计数、时间与内存分别注明范围。14 项旧测试通过不代表评价器正确。

不下载数据、不安装依赖、不生成预留种子、不修 CUDA 驱动、不宣称论文新颖性。UCL 阻塞和 TUM 未下载状态不变。

# 已暴露结果后的法向单参数对照

初轮运行 observation-model-v1-7riqb9c4 发现 cy_wall 的 5/6秩小刚体解出现约12cm平移，空间留出仍通过。追加构造用于归因，不把这些片区变回未见测试。

使用完全相同保存输入、同一参考站、16近邻参考平面、匹配掩码和空间划分。只将6参数运动模型换为1参数法向平移：p' = p - b n_ref。n_ref 从参考站当前点云 PCA 计算。Huber IRLS仍8次；同样报告无确认与双向空间确认版本。没有添加位移上限或使用注入大小限幅。

沿用初轮5对照输出，不重新改动。保存追加输出并与未扰动测量云评分。额外检查输入重复：初轮三个种子在平移/旋转的二元方向抽样中得到同一符号，因此这几类各18行只有6种独特几何输入；不得报告为18次独立成功。

相同观测的单面偏差/真实双层歧义案例再次测试。不能因为单参数更稳定就宣称解决关联错误。

# 预实验协议（先落盘，后运行）

主机：`liekkas`。日期：2026-09-12。本文件写完之前不得写正式 `PILOT_RESULTS.csv`。

## 对象

检查已有四种冻结方法能否吃下「带扫描来源的片区」和「精确合成对照」。不在本轮改算法，不按方法输赢删研究分支。

比较方法（只读导入旧 `operators.estimate`）：

- `identity`
- `xyz_mixture`
- `fast`（自研 profile，不是 JRMPC/BALM）
- `open3d_icp_then_xyz`（官方点到点 ICP + 自研标量后处理，不是 JRMPC/BALM）

输入只有 `xyz_mm`、`frame`、`sigma_mm`。第三轴是片区共同法向。真实片区的局部坐标只从当前算法可见点估计，同一案例的全部方法共用这一套坐标。

## 片区怎么选

先看几何和站间重叠，再提取。不看哪个方法好看。

计划使用：

- ETH3D `delivery_area` clean：`da_wall`、`da_junction`、`da_thin`
- ETH3D `courtyard` clean：若实际有 ≥2 站重叠，同样提取三片
- 合成：可辨识单层重影、可辨识双层（2/4/8 mm，偏差 RMS 0/4 mm，种子 912101/912113/912127），以及不可辨识对照

UCL Basic Corridor 若本轮进不来，真实片区只走 ETH3D，并在结果里记阻塞，不编站号。

每片目标 300–3000 点，保留 `scan_id` 和 `source_point_index`。先用一片计时，再跑其余；不跑整场景优化。

## 扰动

同一真实片区保留点集合和来源，另建副本：

1. 无额外扰动
2. 每站共同平移，点加权 RMS 5 mm，种子 912401/912409/912419
3. 同样平移 + 绕固定片区中心的小角旋转，角度 RMS 0.05°，同样三种子

参考站取最小 `scan_id`，该站不加额外刚体。额外运动不是完整传感器位姿误差模型。原始已对齐位姿和扰动逆变换只进 `evaluation/`。

## 噪声协议

- 合成：提供 σ = 1 mm，标记 `supplied-noise`
- 真实：预先固定 σ = 2 mm 和 5 mm，只做接线与敏感性，不叫传感器真噪声
- 本轮不做盲估计

## 评价（四张分开的表）

1. 合成精确几何：误拆、误并、层距误差、表面位置误差、覆盖。对不上层就记失败，不当零误差。不可辨识案例不按唯一真值打分。
2. 真实扰动恢复：相对未扰动测量云的点误差；另列相对该方法自己无扰动输出的稳定性。参考本身含测量误差。
3. 真实独立质量：填「本轮尚未测得」。不用 BIM，不用 clean 当毫米真值。
4. 成本：单片区时间、总时间、峰值 RSS、点数、站数。下载/解包/邻域/算子分开记。

失败、超时、未处理都留在分母里。

## 计算预算

CPU，现有 Open3D 环境。可选：用现有 PyTorch 做一次小张量核验。`nvidia-smi` 失败不等于 CUDA 不可用。不编 GPU 加速比。

预算：合成约 24+3 个案例 × 4 方法；每个真实片区 × 2 个 σ ×（1 个未扰动 + 2 类扰动 × 3 种子）× 4 方法。超时记失败，不重调参。

# V20：候选继承修复与固定 MAP 的候选余量结算

2026-09-13，liekkas。算法、评分及本协议在读取新确认结果前冻结。

## 问题与比较

1. 恢复同数据的强门控拟合后，V19A的同样本控制是否恢复？
2. 各交叉折仅用训练折数据重建V18完整候选链后，crossfit是否改善？
3. 固定MAP导出，当前S/C/L/R与旧空间候选中是否已有更好几何但未被选中？

原V18/V19只读。V20保留同一V18选定家族、给定sigma、三折XY划分、最终V19几何求解和MAP动作。
同样本gate直接复用本数据的V18家族最优保留候选；每个crossfit训练折独立运行V18候选构造，
在固定家族内保留较优候选。禁止将全数据gate或几何参数传入训练折拟合；
全数据家族选择及最终geometry初始化仍来自V18，沿用V19边界，不声称全流程独立。
原始V19两种输出在本轮同数据重跑作对照，不只比较修复前后汇总。

候选池包括S/C/L/R、旧空间输出和V18 retained；R内另保留有资格的旧空间解。
统一按MAP输出，真值oracle仅事后按来源表面等权MAE选一个候选，
该同一候选报告全部次指标。不逐指标分别挑解，不把oracle当部署成绩。
identity另列对照，不纳入拟合候选oracle。

## 数据和主终点

- 暴露诊断30例：V19统计开发seed9190101的26条件，加4个已定位退化例
  seeds9191101/9191111/9191121/9191131，gap8 sigma1 ratio0.5 dependence1。
- 新确认52条件：seeds9201101、9201111，各26个条件。
- 每例768点。gap0/2/8mm，sigma1/2mm；双层ratio0.2/0.5/0.8、dependence0/1；
  单层仅ratio0.5、dependence0。沿用V19统计生成器；不是新增真实场景或首回波扫描。
- 主终点：来源表面等权MAE。次指标：最近表面MAE、对应RMS、来源间距误差、
  1mm覆盖及跨中面比例。分别报告单/双层及每种子，不以一个平均指标宣布全面胜出。
- 同目标检查仅意味着搜索不退化，不意味着几何风险不退化。
- 不设事后胜出阈值：若修复仅恢复已有解而未优于V18，记为修复而非新精度贡献。
- 两个新种子仅为同族初步确认；本轮不升级真实地图/3DGS主张。

## 记录与执行

只用CPU 4进程并行计算，不创建新代理任务。记录完整候选、各训练折诊断、
旧/新输出、输入及源码hash；各阶段先封存全部预测，再读取GT评分。
补跑的完整候选明确标为V20新拟合，不冒充V19历史缓存。
检查旧缓存/输出的数值重放一致性，零假设检验不读取GT。
大文件使用/srv的新唯一run目录；历史文件只读；不自动git commit或push，不写导师简报。
